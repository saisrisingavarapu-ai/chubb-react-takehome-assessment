# React Coding Standard Rules

- Use ESLint + Prettier for code quality and formatting.
- Use PascalCase for React component names.
- Use camelCase for hooks and utility functions.
- Follow React hook rules strictly (only call hooks at the top level and from React functions).
- Apply DRY principles to avoid repeated logic and duplication.
