# Styling Rules

- Use SCSS for theming.
- Store primitives, semantic combinations, and theme definitions in separate files.
- Handle theme toggling through a simple Theme helper.
- Do not hardcode colors in components.
