# Architecture Rules

src/
  assets/
  components/
  pages/
  store/          # Zustand slices
  styles/         # SCSS themes
  utils/          # Small helpers (formatting, constants)
  i18n/
  tests/
  App.jsx
  main.jsx

Components = UI, store = state, styles = SCSS, utils = helpers.
